dev:
  commands:
  - command: yarn install --frozen-lockfile
    directory: sage-db-search
    name: install cmd
  name: ""
  packages:
  - nixpkgs.nodePackages.yarn
  - nixpkgs.nodejs_18
  repos:
  - https://github.com/tevko/sage-db-search
